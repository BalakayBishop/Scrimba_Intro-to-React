export default function Header() {
    return (
        <header className="header">
            <nav className="header-nav">
                <img src="/src/assets/react.svg" alt={"react icon"} className={"nav-img"} />
                <span className={"nav-text"}>ReactFacts</span>
            </nav>
        </header>
    )
}